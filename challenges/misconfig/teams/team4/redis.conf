# FEIT CTF Redis Configuration
# Intentional misconfiguration: No authentication, bind to all interfaces

bind 0.0.0.0
port 6379
protected-mode no
daemonize no

# Persistence (not strictly needed but good to have)
dir /var/lib/redis
dbfilename dump.rdb

# Logging
logfile ""
